# Task F01-T15 — ADRs: multi-provider architecture + skill-compile model

**Feature:** F01
**Wave:** 4
**Type:** docs
**Depends on:** (none — can run once design is locked; scheduled Wave 4)
**Status:** done

## User Story

As a future maintainer, I want ADRs capturing the multi-provider and
skill-compile decisions, so that the rationale, trade-offs, and non-goals are
durable.

## Objective

Write two ADRs under `docs/adr/` (sequential numbering after `0001-*`)
documenting the provider-driver architecture and the neutral-skill-compile model.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/05-docs-guardrails.md` (section "ADRs").
Use `docs/adr/0000-template.md` for structure.

- Write into BOTH the framework repo `${DIDIO_HOME:-~/.claude-didio-config}/docs/adr/`
  and (if it is the shipping repo) the plan project
  `/Users/eduardodidio/the-grey-havens-config/docs/adr/`. Next numbers after the
  existing `0001-adopt-claude-didio-framework.md` → `0002-*`, `0003-*`.
- ADR A (multi-provider): context = Claude-only coupling in
  `didio-spawn-agent.sh`; decision = pluggable `drivers/<provider>-driver.sh`
  behind spawn-agent, default `claude`, Codex via `codex exec`; consequences =
  backward compat (AC3), NDJSON schema divergence handled by the normalizer
  (T11), Codex has no fallback/effort flags (documented gaps); non-goals =
  API path, Python rewrite, providers beyond Claude+Codex.
- ADR B (skill-compile): decision = neutral `skills/` source compiled to Claude
  (`.claude/commands` + `.claude/agents` + `agents/prompts`) and Codex
  (`~/.codex/prompts` + `AGENTS.md`); source-checked-in / outputs-generated;
  idempotency; subagents skipped on Codex; wired into `sync-project` (T14).

## Implementation details

- File: `docs/adr/0002-multi-provider-driver-architecture.md`
- File: `docs/adr/0003-neutral-skill-compile-model.md`
- Follow the template sections (Status, Context, Decision, Consequences).

## Testing

- Framework: docs lint / link check (no code tests).
- Command: `bash ${DIDIO_HOME}/tests/F01-docs-check.sh` (shared with T16/T17 —
  whichever lands first creates it).
- Test file: `${DIDIO_HOME}/tests/F01-docs-check.sh`
- Convention: assert files exist, have the ADR section headers, status set.

## Test scenarios

- **Happy path:** both ADRs exist with Status/Context/Decision/Consequences.
- **Edge case:** numbering is sequential and unique (no collision with `0001`).
- **Error scenario:** a missing required section is caught by the docs check.
- **Boundary values:** non-goals explicitly list API-path / Python-rewrite /
  extra-providers (grep asserts presence).

## Diagrams

- References `docs/diagrams/F01-architecture.mmd` (owned by T16); no diagram edit
  here.

## Notes from Developer

- Framework repo (`$DIDIO_HOME`) is `/Users/eduardodidio/claude-didio-config`
  (resolved from env), and `0002`/`0003` were already taken there
  (`0002-canonical-project-layout.md`, `0003-sync-hooks-merge.md`). Mirrored
  ADRs into the framework repo as `0004-multi-provider-driver-architecture.md`
  and `0005-neutral-skill-compile-model.md`. Shipping repo
  (`the-grey-havens-config`) uses `0002`/`0003` as planned.
- Created `${DIDIO_HOME}/tests/F01-docs-check.sh` (did not exist yet); asserts
  ADR sections/status, sequential numbering in the shipping repo, and presence
  of the three non-goal phrases in ADR-0002. `PASS: F01 docs check`.
