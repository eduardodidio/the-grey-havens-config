# Task F01-T17 — README + CLAUDE.md multi-provider docs

**Feature:** F01
**Wave:** 4
**Type:** docs
**Depends on:** F01-T12
**Status:** done

## User Story

As a user, I want README + CLAUDE.md to document multi-provider usage, so that I
can assign a role to Codex, compile skills, and meet Codex prerequisites.

## Objective

Update `README.md` with a multi-provider section and update `CLAUDE.md`'s Stack
line + a multi-provider note, per the mandatory CLAUDE.md doc rules.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/05-docs-guardrails.md` (section
"README + CLAUDE.md"). This satisfies the CLAUDE.md "README.md auto-update" rule.

- Files: `${DIDIO_HOME:-~/.claude-didio-config}/README.md` and the same project's
  `CLAUDE.md`; also the plan project
  `/Users/eduardodidio/the-grey-havens-config/{README.md,CLAUDE.md}` if it is the
  shipping repo. The plan project's `CLAUDE.md` Stack line is currently
  "**Blank / custom**" / the framework one says Claude-only — update to reflect
  multi-provider (Claude + Codex).
- README section must cover: setting `models.<role>.provider = codex`; the
  `providers` registry; `didio compile-skills [--target ...]`; `didio providers
  list|validate`; Codex prerequisites (install + auth `codex`, `~/.codex/config.toml`);
  and the documented gaps (no Codex fallback/effort; subagents Claude-only;
  rate-limit reset may be "n/a" for Codex — AC4 degradation).
- Keep `CLAUDE.md` "Guardrails de Segurança" intact (do not edit that section).

## Implementation details

- `README.md`: add a "Multi-provider (Claude + Codex)" section.
- `CLAUDE.md`: update Stack line; add a short "Providers" note linking the ADRs
  (T15) and diagrams (T16). Do not touch guardrails.

## Testing

- Framework: docs check (no code tests).
- Command: `bash ${DIDIO_HOME}/tests/F01-docs-check.sh`
- Test file: `${DIDIO_HOME}/tests/F01-docs-check.sh`
- Convention: grep README for the new section + the `compile-skills`/`providers`
  commands; grep CLAUDE.md for the updated Stack note; assert guardrails section
  still present.

## Test scenarios

- **Happy path:** README has the multi-provider section naming both new
  subcommands and Codex prerequisites.
- **Edge case:** CLAUDE.md Stack line updated; guardrails section byte-unchanged
  (grep proves it still exists).
- **Error scenario:** docs check fails if the README section or a command
  reference is missing.
- **Boundary values:** AC4 documented gaps (no fallback/effort, subagent skip,
  rate-limit n/a) are present in the README.

## Diagrams

- Links to `docs/diagrams/F01-architecture.mmd` + `F01-journey.mmd` (owned by T16).
