# Task F01-T16 — Finalize architecture + journey diagrams

**Feature:** F01
**Wave:** 4
**Type:** docs
**Depends on:** (none — scheduled Wave 4 so diagrams reflect shipped design)
**Status:** done

## User Story

As a maintainer, I want living architecture and user-journey diagrams for F01, so
that the multi-provider data-flow and the run-a-Wave journey are documented per
the CLAUDE.md doc rules.

## Objective

Validate and finalize `docs/diagrams/F01-architecture.mmd` and
`docs/diagrams/F01-journey.mmd` (Architect seeded stubs) so they match the
shipped implementation.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/05-docs-guardrails.md` (section
"Diagrams"). The Architect already created both `.mmd` files at
`docs/diagrams/F01-architecture.mmd` and `docs/diagrams/F01-journey.mmd`.

- This task OWNS both diagrams. Verify they reflect final names/flow after T05–T14:
  config→config-lib→spawn-agent→driver(claude|codex)→JSONL→normalizer→
  {meta/state.json, dashboard, error/rate-limit}; and skills→compiler→
  {.claude/*, ~/.codex/*}→sync-project.
- Journey: swimlanes User / didio CLI / Provider CLI, including the preflight
  abort branch (missing/unauthed provider binary).
- Templates for reference: `docs/diagrams/templates/{architecture.mmd,
  user-journey.mmd}`. Keep Mermaid valid (renders without error).
- Mirror into the framework repo `${DIDIO_HOME}/docs/diagrams/` if that is the
  shipping repo.

## Implementation details

- File: `docs/diagrams/F01-architecture.mmd` — reconcile node labels with final
  script/file names.
- File: `docs/diagrams/F01-journey.mmd` — reconcile lanes/branches with T12/T13.
- At least the two mandatory diagrams; a sequence diagram for the driver dispatch
  is welcome but optional.

## Testing

- Framework: mermaid syntax check (no runtime).
- Command: `bash ${DIDIO_HOME}/tests/F01-docs-check.sh` (validates `.mmd` parse
  if a mermaid CLI is available; else asserts `flowchart`/`subgraph` structure).
- Test file: `${DIDIO_HOME}/tests/F01-docs-check.sh`
- Convention: grep for required nodes (drivers, compiler, normalizer, preflight).

## Test scenarios

- **Happy path:** both `.mmd` files exist and contain the provider-driver split
  and skills-compiler flow.
- **Edge case:** journey includes both the success path and the preflight abort
  branch.
- **Error scenario:** a malformed Mermaid block is caught by the check (negative
  fixture or mermaid-cli parse).
- **Boundary values:** node labels reference real file names (drivers/,
  didio-compile-skills.sh) — grep asserts.

## Diagrams

- `docs/diagrams/F01-architecture.mmd` — OWNER (finalize).
- `docs/diagrams/F01-journey.mmd` — OWNER (finalize).
