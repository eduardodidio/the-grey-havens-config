# Task F01-T05 — Extract Claude invocation into drivers/claude-driver.sh (verbatim)

**Feature:** F01
**Wave:** 1
**Type:** backend (execution)
**Depends on:** F01-T02
**Status:** done

## User Story

As a framework maintainer, I want the current `claude -p` invocation extracted
into a driver that behaves identically, so that the Claude path is preserved
byte-for-byte while becoming pluggable.

## Objective

Create `drivers/claude-driver.sh` reproducing today's `claude -p` call exactly,
consuming the T02 contract env vars.

## Dev Notes

Veja `_brief/00-overview.md` e `_brief/01-execution-provider-abstraction.md`
(section "Current invocation"). Read `${DIDIO_HOME}/drivers/DRIVER_CONTRACT.md`
from T02.

- **Verbatim** = reproduce lines ~117–127 of today's `didio-spawn-agent.sh`:
  ```bash
  set +e
  claude \
    -p "$DIDIO_PROMPT" \
    --output-format stream-json \
    --verbose \
    ${DIDIO_MODEL:+--model "$DIDIO_MODEL"} \
    ${DIDIO_FALLBACK:+--fallback-model "$DIDIO_FALLBACK"} \
    --dangerously-skip-permissions \
    > "$DIDIO_LOG_FILE" 2>&1
  EXIT_CODE=$?
  set -e
  exit $EXIT_CODE
  ```
  The live script does NOT pass `--effort`/`--allowedTools` today — DO NOT add
  them (that would break AC3). `DIDIO_EFFORT` is accepted but unused by this
  driver (documented).
- This task only creates the driver. spawn-agent still calls `claude` inline
  until T07 swaps it — so DO NOT edit spawn-agent here (keeps Wave 1 parallel).
- `chmod +x drivers/claude-driver.sh`. Inherit PATH only (clean env, like today).

## Implementation details

- File: `${DIDIO_HOME}/drivers/claude-driver.sh` — `#!/usr/bin/env bash`,
  `set -uo pipefail` (note: NOT `-e` around the CLI call, mirror today's
  `set +e`/`set -e` dance to capture exit code).
- Read inputs strictly from the contract env vars; no positional args.

## Testing

- Framework: bash smoke test with a **stubbed `claude`** on PATH.
- Command: `bash ${DIDIO_HOME}/tests/F01-claude-driver.sh`
- Test file: `${DIDIO_HOME}/tests/F01-claude-driver.sh`
- Convention: create a fake `claude` script in a temp dir, prepend to PATH, that
  echoes canned stream-json and exits with a chosen code; assert the driver
  forwards flags and writes the log. No real model spend.

## Test scenarios

- **Happy path:** stub `claude` receives `-p`, `--output-format stream-json`,
  `--verbose`, `--dangerously-skip-permissions`; NDJSON lands in `$DIDIO_LOG_FILE`;
  exit 0.
- **Edge case:** empty `DIDIO_MODEL` → no `--model` flag emitted (the `${VAR:+}`
  expansion is exercised).
- **Error scenario:** stub `claude` exits 1 → driver exits 1.
- **Boundary values:** flag set captured by the stub matches today's invocation
  exactly (golden string compare → protects AC3).

## Diagrams

- `docs/diagrams/F01-architecture.mmd` — Exec subgraph (created in T16).

## Notes from Developer

`drivers/DRIVER_CONTRACT.md` and `drivers/` scaffold from T02 were not present
in the repo at the time of this run (T02 marked done but artifacts missing).
Proceeded using the contract documented in
`_brief/01-execution-provider-abstraction.md` ("Driver contract" section),
which fixes the same env var names used here. Created `drivers/` directory as
part of this task.
