# Task F02-T08 — Simulation: dispatch / resolution / meta / failure (Axis C)

**Feature:** F02
**Wave:** 2
**Type:** test
**Depends on:** F02-T03 (harness), F02-T04 (config-lib hardened), F02-T05 (meta hardened)
<!-- readiness: tests/lib/sim-harness.sh is read-only (sourced) in Wave 2 — created by T03 in Wave 0 -->
**Status:** done
**Maps to AC:** AC6

## User Story

As a maintainer, I want a simulation that proves per-role model/provider/
effort resolution and the meta lifecycle (including the failure path) end to
end, so that agent-management behavior is regression-protected with zero model
spend.

## Objective

Add `tests/F02-sim-dispatch.sh` that, via the Wave-0 harness + `echo-driver`,
verifies resolution-per-role flows into the driver env and that `.meta.json`
transitions running→completed (and →failed on non-zero exit). **Extends**, not
duplicates, `tests/F01-spawn-dispatch.sh`.

## Dev Notes

Veja `_brief/03-simulation-tests.md`.

- F01-spawn-dispatch already covers: echo dispatch, NDJSON in log, unknown
  provider exit 2, non-zero exit → failed. **Do not repeat those.** New value
  here: **resolution per role** (model/fallback/effort) + **economy switch** +
  **meta field completeness**.
- `echo-driver` writes one NDJSON line echoing
  `DIDIO_ROLE/FEATURE/TASK_ID/MODEL/FALLBACK/EFFORT` — assert those reflect the
  configured values per role.
- Use `tests/lib/sim-harness.sh` (`sim_make_project`, `sim_spawn`,
  `sim_meta_field`, asserts).
- Build a config where, say, `developer` → model `sonnet`, fallback `haiku`,
  effort `medium`, provider `echo`; and an economy variant where the same role
  → `haiku`. Spawn and read back the echoed line + meta.

## Implementation details

- `tests/F02-sim-dispatch.sh`:
  - Scaffold a temp project (echo provider) for roles architect, developer.
  - **Resolution:** spawn developer → echo NDJSON contains `"model":"sonnet"`,
    `"fallback":"haiku"`, `"effort":"medium"`; meta `model/fallback_model/
    provider` match.
  - **Economy:** flip `economy:true` in the temp config, re-spawn → model
    resolves to the economy tier (`haiku`).
  - **Meta lifecycle:** assert initial+final meta valid JSON; `status` ends
    `completed`; `feature/role/task/log/exit_code/finished_at` present.
  - **Failure path:** `sim_spawn ... 1` (ECHO_DRIVER_EXIT=1) → meta
    `status:"failed"`, `exit_code:1`, spawn-agent non-zero.
  - Cleanup via `trap`.

## Acceptance criteria

- [ ] Per-role resolution (model/fallback/effort) is asserted from the
      echo-driver output AND the meta file, for ≥2 roles.
- [ ] Economy mode resolves the economy-tier model for the same role.
- [ ] Meta lifecycle running→completed verified with all fields populated and
      valid JSON.
- [ ] Failure path (exit 1) → meta failed + propagated exit code.
- [ ] Test uses only `echo-driver` (no model spend); deterministic; passes
      under `bash tests/run.sh`.

## Testing

- Framework: bash. Primary command: `bash tests/F02-sim-dispatch.sh`; also run
  via `bash tests/run.sh`.
- Scope: per-role resolution (model/fallback/effort) asserted from echo-driver
  output AND meta for >=2 roles; economy-tier switch; meta lifecycle completeness.
- Edge/error: `economy=true` resolves the economy tier; `ECHO_DRIVER_EXIT=1`
  yields meta `failed` + propagated non-zero exit; deterministic, zero spend.

## Test scenarios

- **Happy path:** developer spawn → correct model/fallback/effort + meta
  completed.
- **Edge case:** economy=true → economy model tier resolved.
- **Error scenario:** ECHO_DRIVER_EXIT=1 → meta failed, exit≠0.
- **Boundary:** a role with no `effort` set → driver receives empty effort and
  no spurious flag; meta defaults applied.

## Diagrams

- Validates the flow drawn in `docs/diagrams/F02-journey.mmd` (T13).
