# F03-T02 — Align F01-spawn-dispatch test to the canonical driver

**Wave:** 1
**Type:** test
**Depends on:** F03-T01
**Status:** planned

## User Story

As a maintainer, I want `tests/F01-spawn-dispatch.sh` to assert against the
canonical echo-driver schema and the contract's exit variable, so that the
suite verifies real dispatch behavior and stops failing on stale expectations.

## Objective

Corrigir os 5 asserts que falham em `tests/F01-spawn-dispatch.sh` alinhando-os
ao `drivers/echo-driver.sh` (schema) e ao `DRIVER_CONTRACT.md` (var de exit).
**Não tocar** produção (`bin/didio-spawn-agent.sh`) nem o driver.

## Dev Notes

Veja `_brief.md` e `F03-root-cause.md` (produzido por F03-T01). Este é o ÚNICO
arquivo que esta task pode editar: `tests/F01-spawn-dispatch.sh`.

O driver canônico emite (uma linha NDJSON, `drivers/echo-driver.sh:11`):
```
{"type":"system","subtype":"echo-driver","role":"developer","feature":"F01","task":"task","model":"sonnet","fallback":"haiku","effort":"medium"}
```
A var de exit canônica é `ECHO_DRIVER_EXIT` (`drivers/echo-driver.sh:15`,
`DRIVER_CONTRACT.md:33`).

Convenção de teste do projeto: bash, helpers `assert_eq`/`assert_contains` já no
arquivo; rodado por `tests/run.sh` (discovery `tests/F0*-*.sh`). Guardrail: não
mudar produção para "fazer passar" — a direção (confirmada em T01) é alinhar o
teste ao contrato.

## Implementation details

Editar `tests/F01-spawn-dispatch.sh`:

1. **Linha 72** (Categoria A — schema `type`): trocar o needle
   `'"type":"echo"'` por `'"subtype":"echo-driver"'`. Ajustar a descrição do
   assert se mencionar `type:echo`.
2. **Linha 73** (Categoria A — campo `task`): trocar
   `'"role":"developer","feature":"F01","task_id":"task"'` por
   `'"role":"developer","feature":"F01","task":"task"'`.
3. **Linha 149** (Categoria B — var de exit): trocar `DIDIO_ECHO_EXIT_CODE=1`
   por `ECHO_DRIVER_EXIT=1`. Nenhuma outra mudança no bloco boundary — os
   asserts 158–160 (`exit 1`, `status:failed`, `exit_code:1`) devem agora passar
   porque a produção já propaga corretamente (verificado em T01).

Não alterar comentários do cabeçalho que descrevem intenção, exceto se citarem
o schema antigo (`type:echo`/`task_id`); nesse caso atualizar para refletir o
contrato real.

## Acceptance criteria

- [ ] `bash tests/F01-spawn-dispatch.sh` → `PASS: all F01-spawn-dispatch checks passed`, exit 0.
- [ ] Os 5 asserts antes falhando agora passam (2 de schema + 3 de exit/meta).
- [ ] `git diff` toca **somente** `tests/F01-spawn-dispatch.sh`.
- [ ] `drivers/echo-driver.sh`, `drivers/DRIVER_CONTRACT.md` e
      `bin/didio-spawn-agent.sh` permanecem byte-idênticos.
- [ ] `bash tests/F02-sim-dispatch.sh` e `bash tests/F02-sim-parallel.sh`
      continuam passando (mesmo schema canônico).

## Testing

Framework: bash test harness do projeto. Comandos:
```bash
bash tests/F01-spawn-dispatch.sh      # alvo desta task
bash tests/F02-sim-dispatch.sh        # regressão F02 (mesmo schema)
bash tests/F02-sim-parallel.sh        # regressão F02
bash tests/run.sh                     # gate global (deve subir para 13+ passed)
```
Sem mocks novos; o teste já cria fixtures em `$TMP` e usa `provider:"echo"` com
`bin:"true"`.

## Test scenarios

- **Happy path:** role `developer` → provider `echo` → NDJSON contém
  `"subtype":"echo-driver"` e `"task":"task"`; meta `provider:echo`,
  `status:completed`, exit 0.
- **Edge (AC3):** role sem provider → `claude` com o golden flag-set (assert
  inalterado, deve continuar verde).
- **Error:** provider inexistente → exit 2 + mensagem clara (assert inalterado).
- **Boundary (Categoria B):** `ECHO_DRIVER_EXIT=1` → spawn-agent exit 1, meta
  `status:failed`, `exit_code:1`, `finished_at` setado.

## Diagrams

Nenhum nesta task (diagramas em F03-T04). A mudança de schema/exit alimenta o
diagrama `F03-architecture.mmd`.
