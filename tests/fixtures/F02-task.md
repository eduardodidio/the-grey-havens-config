# Task F02-T00-sim — Minimal fixture task (simulation tests)

**Feature:** F02
**Wave:** 0
**Type:** fixture
**Status:** fixture

## Objective

Minimal valid task file used by `tests/lib/sim-harness.sh` smoke tests and
`tests/F02-sim-*.sh` to exercise `didio-spawn-agent.sh` without real model
spend.

## Acceptance criteria

- [ ] Agent receives this task body in its prompt.
